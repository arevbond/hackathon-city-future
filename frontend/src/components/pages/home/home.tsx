import { Container } from '../../container/container';
import { Layout } from '../../layout/layout';

export const Home = () => {
	return (
		<Container>
			<Layout>
				<div>Home</div>
			</Layout>
		</Container>
	);
};
